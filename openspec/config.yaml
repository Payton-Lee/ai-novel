schema: spec-driven

context: |
  Language: Chinese
  All artifacts must be written in Chinese.
  Keep OpenSpec structural headings and SHALL/MUST keywords in English.
  项目是 Python/LangGraph + Next.js/React/TypeScript 的中文 AI 小说创作平台。
  保留现有工作流与 SQLite，通过增量改造连接导演、设定库和连载引擎。
  作者体验优先：同一本书内完成设定、大纲、正文、修改和续写。
  观察到的实现、待验证假设、拟新增功能必须区分；不能将规划完成当作功能完成。
  后端使用 uv、pytest、Ruff、mypy；前端在 web/ 使用 pnpm、ESLint、Prettier。
  前端改动遵循 web/AGENTS.md；模型评审分数不得描述为真实读者留存预测。

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-operation guidance (optional)
# Add advisory guidance for how apply and archive work should be conducted.
# This is separate from artifact rules above.
# Example:
#   operations:
#     apply:
#       guidance:
#         - Keep test summaries concise
#     archive:
#       guidance:
#         - Summarize the archive outcome before finishing
